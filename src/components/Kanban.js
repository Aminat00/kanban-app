import React, { useEffect, useState } from "react"
import axios from "axios"

const Kanban = () => {
	const [events, setEvents] = useState([])

	return (
		<div>
			<h3>Kanban board should be here</h3>
		</div>
	)
}

export default Kanban
